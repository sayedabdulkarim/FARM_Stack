annotated-types          # supprt for typ annotatns
anyio                    # async stuff, makes io ops fastr
certifi                  # ssl cert managr
charset-normalizer       # fixes weird txt encodings
click                    # makes cli tools ez
dnspython                # dns lookups n stuff
ecdsa                    # crypto signatures
email_validator          # cheks if emails r legit
exceptiongroup           # grps error handling togthr
fastapi                  # quick api buildr
fastapi-cli              # cmd tools 4 fastapi
h11                      # basic http stuff
httpcore                 # core http things
httptools                # fast http parsing
httpx                    # modern http client, bttr than requests
idna                     # handles weird urls
Jinja2                   # templte engine
markdown-it-py           # markdown -> html
MarkupSafe               # keeps templtes safe
mdurl                    # markdown url helper
orjson                   # supr fast json stuff
passlib                  # pwd hash n security
pyasn1                   # encoding format thing
pydantic                 # data validatn made ez
pydantic-settings        # config mgmt
pydantic_core            # core pydantic stuff
Pygments                 # code highlightr
pymongo                  # talks 2 mongodb
python-dotenv            # env vars made ez
python-jose              # jwt n security stuff
python-multipart         # handles file uploads
PyYAML                   # yaml file handler
requests                 # old skool http client
rich                     # fancy terminal output
rsa                      # encryption stuff
shellingham              # finds which shell ur using
six                      # py2/3 compatibility
sniffio                  # async detectn
starlette                # fastapi foundation
typer                    # cli builder
typing_extensions        # extra type hints
ujson                    # fast json parser
urllib3                  # http client lib
uvicorn                  # fast web servr
uvloop                   # fast event loop
watchfiles               # watches 4 file changes
websockets               # websocket support
